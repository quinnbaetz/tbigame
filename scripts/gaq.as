import flash.external.ExternalInterface;function startTrack(task){	var startTime = flash.utils.getTimer();	return function(){		var endTime = flash.utils.getTimer();		if(endTime > startTime){			trackTime(task, endTime-startTime);		}	}}function trackTime(task, time){	ExternalInterface.call('gaq', '_trackTiming', ['tbigame_task_completion', task, time, '', 100]);}//Trackers:	//Tracking each time we go to a different timeline scene	//Tracking how many clicks it takes for a person to finish the ct testfunction trackPageView(page){	ExternalInterface.call('gaq', '_trackPageview', [page]);}function trackEvent(category, action, opt_label, opt_value){	ExternalInterface.call('gaq', '_trackEvent', [category, action, opt_label, opt_value]);}